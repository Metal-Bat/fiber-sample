LoginResponse:
  description: Login successful

  content:
    application/json:
      schema:
        $ref: "../base/schemas.yaml#/LoginResponse"

UsersPaginatedResponse:
  description: Users list

  content:
    application/json:
      schema:
        $ref: "../base/schemas.yaml#/UsersPaginatedResponse"

UsersDetailResponse:
  description: User detail

  content:
    application/json:
      schema:
        $ref: "../base/schemas.yaml#/UsersDetailResponse"

UsersDetailCreateResponse:
  description: User created

  content:
    application/json:
      schema:
        $ref: "../base/schemas.yaml#/UsersDetailCreateResponse"
